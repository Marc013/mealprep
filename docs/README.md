# Strongman Meals

Maaltijdplan & mealprep website — gebouwd met Jekyll en GitHub Pages.

## Lokaal testen (optioneel)

```bash
cd docs
gem install bundler jekyll
bundle init
bundle add jekyll
bundle exec jekyll serve
```

Open daarna `http://localhost:4000` in je browser.
